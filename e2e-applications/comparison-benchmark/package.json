{
  "name": "comparison-benchmark",
  "version": "1.0.0",
  "description": "Benchmark app to compare @nestjs/swagger CLI plugin output with nestjs-openapi-static",
  "scripts": {
    "build": "nest build",
    "generate-swagger": "rm -rf dist && nest build && node dist/main.js"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0",
    "@nestjs/swagger": "^7.0.0",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.0.0",
    "@nestjs/schematics": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
